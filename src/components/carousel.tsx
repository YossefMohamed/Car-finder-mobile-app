import React from "react";
import { Text } from "react-native";
import Carousel from "react-native-snap-carousel";

const CarouselCars = () => {
  const data = [0, 14, 25, 54, 645];
  const carouselRef = React.useRef<any>();
  return <Text>ss</Text>;
};

export default CarouselCars;
